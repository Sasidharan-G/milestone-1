# Test plan

Validate Money and billing calculations, stock ledger writes, repository behavior, permissions, sync states, exports, Room migrations/transactions, API validation/auth, and critical Compose flows. Include offline billing, printer failure, restricted access, and all report export scenarios from the client brief.

